nba_api
psycopg2-binary
python-dotenv
requests
beautifulsoup4
# truth-layer unit tests (scraper/test_truth_layer.py). Pure functions only —
# no database, no network — so this is the whole test dependency.
pytest
